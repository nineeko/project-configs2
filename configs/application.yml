spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

feign:
  client:
    default:
      connectTimeout: 5000
      readTimeout: 5000
      loggerLevel: full

management:
  endpoints:
    web:
      exposure:
        include: refresh

